import test from 'node:test';import assert from 'node:assert/strict';
const state=new Map();global.window={sessionStorage:{getItem:k=>state.get(k)||null,setItem:(k,v)=>state.set(k,v),removeItem:k=>state.delete(k)},location:{href:'https://example.test/liff/referral?ref=code'}};
const {REFERRAL_FLOW_STORAGE_KEY,getReferralFlowToken,setReferralFlowToken,clearReferralFlowToken,applyReferralFlowTerminalResult}=await import('../src/referral-flow.js');
test('carrier stores and restores only the opaque token in sessionStorage',()=>{assert.equal(setReferralFlowToken('opaque.signed.token'),true);assert.equal(state.get(REFERRAL_FLOW_STORAGE_KEY),'opaque.signed.token');assert.equal(getReferralFlowToken(),'opaque.signed.token');assert.equal(window.location.href.includes('opaque.signed.token'),false);assert.equal(state.has('smart_menu_auth_token'),false);});
test('carrier persists through simulated reload and login return without decoding',()=>{setReferralFlowToken('same-flow');assert.equal(getReferralFlowToken(),'same-flow');assert.equal(getReferralFlowToken(),'same-flow');});
test('carrier clears only terminal referral results and retains NOT_FRIEND',()=>{setReferralFlowToken('retry-flow');applyReferralFlowTerminalResult('NOT_FRIEND');assert.equal(getReferralFlowToken(),'retry-flow');applyReferralFlowTerminalResult('QUALIFIED');assert.equal(getReferralFlowToken(),'');setReferralFlowToken('x');clearReferralFlowToken();assert.equal(getReferralFlowToken(),'');});