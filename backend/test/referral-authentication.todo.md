# DEFERRED_MANDATORY_TEST_A3

Before production: verified LIFF plus valid flow writes exactly one LIFF_AUTHENTICATED event; invalid LIFF writes zero; retries do not inflate; no LIFF token, UID, identity hash, or raw flow token persists.