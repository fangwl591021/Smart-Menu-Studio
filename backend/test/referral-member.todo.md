# DEFERRED_MANDATORY_TEST_A4

Before production: valid member resolution plus valid flow writes MEMBER_ESTABLISHED; same-flow retry persists one event; failed member resolution writes none; analytics failure never rolls back member; non-referral flow is unchanged; no UID/hash/LIFF token/raw flow token persists.